export const metadata = {
  title: "Workspace",
  description: "Your ReFile chat workspace — upload files, get commands, download results.",
  robots: { index: false, follow: false },
};

export default function DashboardSegmentLayout({ children }) {
  return children;
}
