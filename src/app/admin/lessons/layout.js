export const metadata = {
  title: "Learned Fixes — Review",
  description:
    "Admin review queue for self-improving prompt lessons distilled from job failures.",
  robots: { index: false, follow: false },
};

export default function AdminLessonsSegmentLayout({ children }) {
  return children;
}
