export const metadata = {
  title: "Presets",
  description:
    "Reusable shell-command recipes for converting, extracting, and transforming files — contributed by the ReFile community.",
  openGraph: {
    title: "Presets — ReFile",
    description:
      "Reusable shell-command recipes for files — image, video, audio, PDF, and more.",
  },
};

export default function PresetsSegmentLayout({ children }) {
  return children;
}
